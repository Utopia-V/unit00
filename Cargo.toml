[workspace]
resolver = "2"
members = ["kernel"]

[workspace.package]
version = "0.1.0"
authors = ["pilot.00"]
edition = "2024"
license = "Apache-2.0"
repository = ""

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
opt-level = "s"
